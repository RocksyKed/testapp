const initialState = {
    center: [46.452210, 30.738623],
    zoom: 13,
    markers: []
};

export const map = (state = initialState, actions) => {
    switch (actions.type) {

        default:
            return state;
    }
};